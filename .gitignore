# pixi environments
.pixi/*
!.pixi/config.toml
*.egg-info

# Python
*.pyc

# Build output
build/*
install/*
log/*
# AIC demo build output (separate tree so it can coexist with the base build)
build_aic/*
install_aic/*
log_aic/*

# IDE metadata
.vscode
node_modules/

# lerobot
logs
outputs

# External repos imported via vcs
external/*
# AIC demo sources imported via vcs (aic.repos)
external_aic/*

# Recorded datasets
datasets/*
datasets_lerobot/*

# wandb
wandb
